# Claude Code Instructions — Stop Slop CN-EN

Use the root `SKILL.md` as the canonical instructions for this repository.

For Claude Code skill installation, copy the packaged skill directory:

```text
skills/creative/stop-slop-cn-en/
```

to:

```text
~/.claude/skills/creative/stop-slop-cn-en/
```

The directory name and the `name:` field are both `stop-slop-cn-en`. Do not reference older repository layouts.
