# Agent Instructions — Stop Slop CN-EN

This repository packages the `stop-slop-cn-en` AgentSkills-compatible skill.

For Codex/OpenAI-style agents:

1. Treat the root `SKILL.md` as the canonical skill instructions when working in this repository.
2. If installing as a reusable skill, copy `skills/creative/stop-slop-cn-en/` to your agent skills directory as `stop-slop-cn-en/`.
3. Do not use stale paths from older layouts; the Hermes-style packaged copy is `skills/creative/stop-slop-cn-en/SKILL.md`.

Follow the privacy, safety, and verification rules in `SKILL.md`.
